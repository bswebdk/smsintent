apply plugin: 'com.android.application'

android {
    signingConfigs {
        release {
            keyAlias 'BSWebDK'
            keyPassword 'KlumpFisk'
            storeFile file('/home/tnb/Dokumenter/develop/android/keystore')
            storePassword 'KlumpFisk'
        }
    }
    compileSdkVersion 19
    buildToolsVersion "24.0.0"
    project.archivesBaseName = "SMSIntent_1.0.3"
    defaultConfig {
        applicationId "com.bswebdk.smsintent"
        minSdkVersion 11
        targetSdkVersion 19
        signingConfig signingConfigs.release
        testApplicationId 'SMSIntent'
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
            signingConfig signingConfigs.release
        }
        debug {
            debuggable true
            minifyEnabled false
        }
    }
    productFlavors {
    }
}

dependencies {
    compile 'com.android.support:support-v4:19.1.0'
    compile 'com.google.android.gms:play-services-appindexing:8.1.0'
}
